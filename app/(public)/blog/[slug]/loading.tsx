import { ArticleSkeleton } from '@/components/shared/page-skeleton';

/** Texto longo: o esqueleto imita a coluna de leitura, nao uma grade. */
export default function Loading() {
  return <ArticleSkeleton />;
}
